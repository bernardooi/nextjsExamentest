export const redirectToHome = {
  redirect: {
    destination: "/",
    permanent: false,
  },
};

export const redirectToLogin = {
  redirect: {
    destination: "/login",
    permanent: false,
  },
};
